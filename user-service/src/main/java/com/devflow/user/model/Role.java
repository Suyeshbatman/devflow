package com.devflow.user.model;

// Simple enum defining the 3 roles in DevFlow
// ADMIN    = platform administrators
// DEVELOPER = regular paying users
// VIEWER   = read-only free tier users
public enum Role {
    ADMIN,
    DEVELOPER,
    VIEWER
}